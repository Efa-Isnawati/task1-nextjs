/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn2.thecatapi.com",
      "image.tmdb.org",
      "encrypted-tbn0.gstatic.com",
    ],
  },
};

export default nextConfig;
